# Real Estate Backend — Project Instructions

Multi-tenant real estate agency platform (FastAPI + PostgreSQL/PostGIS + Redis).
The full blueprint is in [project.md](project.md) — follow it. The build plan is
incremental: one part at a time, never everything at once.

**Codebase context:** `graphify-out/` has the knowledge graph — query it before searching from scratch.

## Per-part workflow

1. Run checks: `uv run pytest` + `uv run ruff check` + `uv run mypy` — all green.
2. Update the **Build progress log** below.
3. Commit.

Code review and graph updates are done manually by the user — don't run them automatically.

## Architecture rules (from project.md §5 — enforced in review)

- Modular monolith under `src/app/modules/<name>/` with `models.py / schemas.py / repository.py / service.py / router.py`.
- Routers never touch the DB: routers → services → repositories.
- Modules never import another module's `models.py`/`repository.py` — call its service or subscribe to its events.
- Every repository method takes `tenant_id`. No exceptions. Postgres RLS is the safety net, not the only guard.
- API responses use explicit `*Out` schemas (`app.core.schema.OutSchema`); inputs use `InputSchema` (`extra="forbid"`).
- Errors leave the API only as RFC 9457 problem+json (raise `AppError` subclasses from `app.core.exceptions`).
- camelCase JSON on the wire, snake_case in Python; cursor pagination (`app.core.pagination`) for all list endpoints.
- Anything > ~200ms of work goes to a background worker (Celery, from Part 5 on).

## Commands

- Deps: `uv sync` · Add dep: `uv add <pkg>`
- Stack: `docker compose -f docker/docker-compose.yml up -d --wait` (Postgres+PostGIS :5432, Redis :6379, Mailpit UI :8025)
- Run: `uv run uvicorn app.main:app --reload --port 8000`
- Worker: `uv run celery -A app.workers.celery_app worker --loglevel info -Q default,media,sync,analytics`
- Beat: `uv run celery -A app.workers.celery_app beat --loglevel info`
- Tests: `uv run pytest` (needs the docker stack; uses `realestate_test` DB + Redis db 1; Celery runs in eager mode, no worker/beat process needed)
- Lint/types: `uv run ruff check` · `uv run ruff format` · `uv run mypy`
- Migrations: `uv run alembic revision --autogenerate -m "..."` then hand-review; `uv run alembic upgrade head`
- The app connects as non-superuser `app_user` (RLS applies); Alembic uses `DATABASE_DDL_URL` (postgres role).

## Build progress log

- **Part 1 — Scaffold & core foundation (done, 2026-07-15):** uv project (`src/app` layout), docker-compose (PostGIS 16, Redis 7, Mailpit) with `app_user` role + `realestate_test` DB via initdb script, `core/` (config via pydantic-settings, structlog logging with PII redaction, RFC 9457 exception handlers, request-id + security-headers ASGI middleware, cursor pagination, camelCase schema bases), `/healthz` + `/readyz`, async Alembic wired to `DATABASE_DDL_URL` (no migrations yet — first one lands with Part 2 models). 10 tests green; ruff + mypy strict clean.
- **Part 2 — Database foundation & tenancy (done, 2026-07-15):** `core/database.py` (async engine + session factory, declarative `Base` with naming convention, UUIDv7 PK + timestamp mixins, request-scoped session that commits at the request boundary and runs `SET LOCAL app.tenant_id` via `set_config` when a tenant is resolved, post-commit callback hook), `core/rls.py` (RLS DDL helpers for future tenant-table migrations — policy reads `current_setting` without `missing_ok`, fail-closed), `core/tenancy.py` (`TenantContext`, pure-ASGI `TenantResolutionMiddleware`: Host → tenant with exempt prefixes, 404 unknown / 402 suspended as problem+json, `TenantDep`), `core/security.py` (interim `X-Platform-Key` guard until Part 3 auth), tenants module (global `tenants`/`tenant_domains` tables — deliberately no RLS since the middleware queries them pre-context; platform CRUD + suspend/activate + domain management with primary-domain rules; Redis-cached `DomainTenantResolver` degrading to DB on Redis failure), public `GET /site/config`, first migration (hand-written, tested via downgrade/upgrade in conftest). Key decisions: cache invalidation runs via post-commit callbacks (pre-commit invalidation let a concurrent reader re-cache stale state for the TTL — review finding); unique-violation `IntegrityError` maps to 409 (pre-checks race under concurrency — review finding); validation errors strip to `type/loc/msg` (pydantic `ctx`/`input` leak PII and non-serializable objects). 27 tests green incl. real-RLS probe suite; ruff + mypy strict clean.
- **Part 3 — Auth, users & RBAC (done, 2026-07-16):** users module (global `users` table with nullable `tenant_id` — NULL = platform staff; identity RLS policy exposes exactly the caller's partition; Argon2id via pwdlib, timing-safe dummy verify against enumeration; soft delete), auth module (`auth_sessions` table; register/login → access JWT ≤15 min + opaque refresh token in httpOnly path-scoped cookie, SHA-256 at rest; rotation with family-wide revocation on reuse — revocation commits on a *dedicated* session because the 401 rolls back the request transaction; password reset + email verification as single-use hashed Redis tokens consumed with GETDEL; Mailpit SMTP integration, fail-soft), `core/permissions.py` (static role→permission matrix in code, `require()` dependency, tid-claim ↔ resolved-tenant pinning so tokens are useless cross-tenant and cross-plane), platform login replacing the X-Platform-Key stopgap, migration 0002, `scripts/create_platform_admin.py`. Key decisions from review: live access tokens are tracked per user in a Redis set (`auth:jti:all:{id}`) so disable/demote/delete/logout-all/password-reset denylist every outstanding jti *immediately* instead of waiting out the 15-min TTL (review finding — "logout-all" and admin-disable imply immediate effect); the users router orchestrates users+auth services for that revocation since auth already depends on users (no circular import); Redis failures degrade auth checks open for signed ≤15-min tokens but never extend refresh (rows are committed). Deferred: MFA/TOTP, OAuth, lockout backoff, proxy-aware client IP (raw peer recorded until an X-Forwarded-For trust boundary exists). 58 tests green; ruff + mypy strict clean.
- **Part 4 — Listings module (done, 2026-07-16):** first RLS-protected tenant business tables via migration 0003 (`listings`, `listing_status_history`, `listing_reference_counters`; PostGIS extension + POINT geometry, GIN on features, GiST on location, hot-path composite indexes), `core/i18n.py` (locale negotiation `?locale=` > `Accept-Language` > default; fallback chain so any translated field always resolves), listings module: portal CRUD + duplicate + status history, workflow transitions (§8.1 graph incl. `archived → draft` relist; publish requires `LISTING_PUBLISH` or agent + tenant `settings.listings.agent_self_publish`), per-tenant per-year reference codes (`AGE-2026-00001`) minted by atomic `INSERT … ON CONFLICT … RETURNING` counter, ownership scoping in the repository (`scope_user_id`: agents see assigned-or-created only; team_lead/admin/marketing tenant-wide; scoped misses are 404 — no existence oracle), public endpoints returning one negotiated locale per i18n field with detail lookup by reference code or UUID, EWKB/WKT point helpers in `geo.py` (no shapely). New permissions `LISTING_MANAGE`/`LISTING_PUBLISH` in the role matrix. Key decisions/findings: `eager_defaults=True` on `Base` (async sessions can't lazy-refresh `updated_at` after UPDATE — serializing a flushed ORM object raised MissingGreenlet); FastAPI 0.139 silently degrades a Pydantic query-param model to a required scalar if any plain `Query()` param sits next to it, so `PublicListingQuery` folds cursor/limit/locale into the filters model; PATCH rejects explicit `null` for NOT NULL columns (422 instead of 500 at flush); review findings applied — `agentId: null` now unassigns (manager-only) instead of silently reassigning to the caller via the create-time default, and transitions/deletes take `SELECT … FOR UPDATE` so concurrent workflow moves re-validate instead of losing updates and forking the history trail. Deferred (per §8): geocoding, media, FTS/search, view-count increments, `expires_at` enforcement (Celery part). 78 tests green; ruff + mypy strict clean.
- **Part 5 — Background workers, Celery + beat (§12; media pipeline deferred to a dedicated part):** `app/workers/` — `celery_app.py` (Redis-backed broker/result-backend, four queues by workload profile — `default`/`media`/`sync`/`analytics` — `task_routes`, `beat_schedule`); `db.py` gives sync Celery task bodies access to async SQLAlchemy: `run_scoped`/`run_scoped_many` open a transaction (with `SET LOCAL app.tenant_id` when scoped) on a short-lived engine, `run_scoped_many` shares one engine across a batch instead of one per call; `run_sync` detects whether an event loop is already running (Celery's `task_always_eager` in tests executes task bodies inline inside pytest-asyncio's loop, where a bare `asyncio.run` would raise) and falls back to a worker thread in that case — a real prefork worker process always takes the plain `asyncio.run` path. Two tasks: `tasks/email.py` (`send_email`, retries w/ backoff) replaces `AuthService`'s inline `await email.send(...)` — `_send` now just enqueues (`app.state.email_service` removed, nothing else used it); `tasks/listings.py` (`flag_stale_listings`, nightly Beat job) iterates every non-suspended tenant and flags published listings past `expires_at` (or past `listing_stale_after_days` when never set) via new `listings.stale_flagged_at` column (migration 0004) — cleared on any `ListingService.update()` patch or on transitioning away from `published`. Idempotent (already-flagged rows excluded from the query, so overlapping/retried runs don't double-count) and tenant-scoped throughout (RLS is fail-closed — no cross-tenant bypass query even from a worker). Key decisions/findings: celery[redis] transitively pins `redis<7`, which downgraded the installed `redis` package and broke one pre-existing mypy line in `auth/service.py` (`smembers` overload ambiguity) — fixed with a scoped `type: ignore[misc]` and a comment, not a code change; celery ships no `py.typed` marker so `@shared_task` decorators are untyped under strict mypy — scoped a `disallow_untyped_decorators = false` mypy override to `app.workers.tasks.*` only; review finding applied — `flag_stale_listings` originally opened a fresh engine per tenant per run, switched to `run_scoped_many` sharing one engine across the whole batch. No app Dockerfile exists yet (app still runs via `uv run uvicorn` locally), so the worker/beat processes are documented as `uv run celery ...` commands in this file rather than added to docker-compose. Deferred to a later part: the full media pipeline (§8.2 — presigned uploads, libvips variants, blurhash, watermarking), portal syndication, and an outbox pattern (no `leads` module yet for it to matter). 85 tests green (incl. eager-mode Celery task tests hitting real Postgres/Redis/Mailpit); ruff + mypy strict clean.
- **Part 6 — Media pipeline (§8.2, done, 2026-07-16):** MinIO added to docker-compose (S3-compatible; a one-shot `minio-init` bootstraps `media`/`media-private` buckets, dev + test, with the `media` bucket set to anonymous-download as a local CDN stand-in); `core/storage.py` (thin boto3 wrapper — path-style addressing for MinIO compat, presigned PUT/GET, `object_size` HEAD check, batch `delete_objects`); `media` module (`models.py` — `ListingMedia` with a partial-unique index enforcing at most one cover per listing; `schemas.py` — upload/embed/update payloads, embed URLs allowlisted by host per kind since they land in an `<iframe>`; `repository.py`; `service.py` — every op resolves the listing through `ListingService.get_portal` first so ownership scoping/404s are inherited, never re-implemented; `router.py`), migration 0005 (`listing_media`, tenant RLS). Upload flow: `POST .../media/uploads` presigns a PUT straight to the private bucket (file never touches FastAPI) → client PUTs → `POST /portal/media/{id}/confirm` flips to `processing` and enqueues `process_media` from a post-commit hook. `workers/tasks/media.py`: `process_media` HEAD-checks real size before buffering (a presigned PUT can't cap Content-Length — declared `sizeBytes` is a client claim, not proof), verifies magic bytes against the declared content type, derives libvips variants (thumb/card/gallery/full × webp/jpeg, `keep="none"` strips all metadata incl. EXIF GPS), computes a blurhash from a 32px render, uploads variants under content-hashed keys; validation failures mark the row `failed` and delete the original — permanent, never retried, vs. infrastructure errors which do retry. `delete_media_objects` cleans up storage on media delete (private original + any variants), enqueued post-commit so a rolled-back delete never orphans-deletes live objects. Public listing endpoints (`listings/router.py`) now carry a `cover` (list + detail) and full `media` gallery (detail only) via `MediaService.covers_for`/`public_for_listing`. Documents/floorplans stay in the private bucket — presigned GET, 15 min, from `MediaService.download_url` — never public. Key decisions/review findings: `ListingService.get_portal` gained a `for_update` flag so the media quota check (count-then-insert) can take the same row lock pattern Part 4 established for workflow transitions, closing a race where concurrent uploads could exceed the photo quota; `storage_access_key`/`storage_secret_key` have no config defaults (matching `app_secret_key`'s fail-fast rule) after review caught a hardcoded MinIO dev credential silently backstopping a missing prod value; `process_media` HEAD-checks size before `get_object` buffers the body, closing an OOM path where a client could PUT an arbitrarily large file past the declared size and have the worker read the whole thing into memory before rejecting it; a variants-orphaned-on-delete-race (media deleted mid-processing, after the delete task already ran against an empty variants dict) is closed by having `_mark_ready` delete its own just-uploaded variants when it finds the row gone, rather than deferring to a sweep job that didn't exist. 95 tests green (incl. real MinIO round-trips: presign → PUT → confirm → eager-mode Celery processing → public fetch); ruff + mypy strict clean.
- **Part 7 — Search & discovery (§8.3, done, 2026-07-16):** search lives *inside the listings module*, not a separate `modules/search` — a search module would have to import listings' models/repository, and the no-cross-module-model-imports rule wins over §5's aspirational layout (extract when Meilisearch arrives). Migration 0006: `featured` bool (paid placement), STORED generated `search_vector` tsvector — all three locales in one vector, each through its own text-search config (french/english/arabic, PG12+; city via `simple`, weights title A > description B > city C; `to_tsvector(regconfig, text)` and `jsonb ->>` are immutable so no trigger needed) — GIN index, and `(tenant_id, status, featured DESC, published_at DESC)` covering the default sort. Public `GET /listings` grew: `q` (websearch_to_tsquery in the *negotiated locale's* config), `features[]` (JSONB `@>` on the GIN index, AND semantics), `areaMin`, `city`, and one-of geo modes `inBbox=minLon,minLat,maxLon,maxLat` / `near=lon,lat&radiusKm` / `inPolygon="lon lat,..."` (parsed+range-checked in `PublicListingFilters` validators into PrivateAttrs; polygon WKT is built only from parsed floats; `near` = GiST-servable `&&` ST_Expand degree prefilter + exact geography `ST_DWithin`; polygon through `ST_CollectionExtract(ST_MakeValid(...), 3)` so self-intersecting drawn shapes degrade instead of erroring). Sorting: `sort=newest|price_asc|price_desc|area_asc|area_desc`, `featured DESC` leads every sort; keyset is (featured, key, id) with the cursor pinning the sort it was minted under (mismatch → 400); area sorts key on `coalesce(area_built,0)` so the nullable column stays total-ordered. `GET /listings/map` (declared before `/{ref_or_id}` — path match is declaration-order): ≤500 hits → `{id,lat,lng,price,status}` pins; beyond → server-side geohash-bucket clusters (centroid+count, precision derived from bbox span). `featured` PATCH is manager-only (agents can't self-feature paid placement). SEO: `GET /api/v1/sitemap.xml` (published listings on the resolved tenant host, ≤50k URLs, XML-escaped) on a new `seo_router`; public detail carries schema.org `RealEstateListing` JSON-LD (`jsonLd`, detail only — list cards stay lean). Gotchas hit: SQLAlchemy refuses `bool_column < python_bool` (keyset's featured-past branch is `is_(False)`/`false()` instead); PowerShell 5.1 strips embedded double quotes in `python -c` args (graphify update steps went through script files). Review (sonnet): zero findings, approved. 109 tests green; ruff + mypy strict clean. Deferred: Meilisearch (>50k listings), view-count/analytics (§8.15), pages/posts in sitemap (content part), canonical slug redirects.
- **Part 8 — Leads & CRM (§8.4, done, 2026-07-17):** one `modules/leads` module, *not* the `leads`/`clients` split §5 lists — every lead has a mandatory `contact_id` and no standalone contact-portal lifecycle exists yet, so the no-cross-module-model-imports rule wins (carve out `clients` later if a contact portal materializes). Migration 0007: five tenant-RLS tables — `contacts` (merge-fill dedupe by lower(email)-then-phone; consent JSONB only ever upgrades), `leads` (source/stage/score/lost_reason/first_response_at), `lead_activities` (append-only unified timeline; NULL actor = system), `assignment_rules` (one row per tenant), `lead_drip_state` (one row per lead). Public `POST /leads/capture` (unauthenticated, camouflaged honeypot — hp hits return a fake id and persist nothing; `renderedAt` min-fill 3s / max-age 24h; Redis sliding-window rate limit in new `core/rate_limit.py`, tenant+IP keyed, degrade-open) → contact dedupe → scoring (source weight + listing-attached + engagement − recency decay, clamped 0–100) → assignment engine (`listing_agent` default via new `ListingService.agent_for()` boundary accessor; `round_robin` least-loaded over pool or all active agents with optional `max_open_leads_per_agent`; `territory` rejected at the write boundary until §8.5 data exists) → drip seed → post-commit speed-to-lead email to the assigned agent. Portal: lead CRUD + stage transitions (`lost` requires a reason; FOR UPDATE row lock), activities (client-loggable types allowlisted; agent-authored touch sets `first_response_at` and stops the drip as a reply proxy), contact timeline (leads+activities merged, ownership-scoped), assignment-rule GET/PUT (`LEAD_ASSIGN`). Beat sweep (15 min, `default` queue — it sends human-facing email): advances due drip steps (tenant-overridable sequence via `settings.leads.drip_sequence`) and escalates >30-min-unassigned NEW leads to admins once (NOT-EXISTS on the escalation activity = idempotent). New permissions `LEAD_MANAGE`/`LEAD_VIEW_ALL`/`LEAD_ASSIGN`. **Bug found by the test suite:** Celery's current-app lookup is thread-local, so `run_sync`'s ThreadPoolExecutor fallback (eager-mode path) resolved `@shared_task.delay()` against an unconfigured *default* Celery app — nested `send_email.delay()` inside the sweep silently sent nothing; fixed in `workers/db.py` by `celery_app.set_current()` inside the worker thread (lazy import — celery_app→tasks→db is circular at module scope). Review findings applied: lead reassignment (`agentId` PATCH) is manager-only mirroring listings; assignment-rule `config` validated by typed `AssignmentRuleConfig` (free-form JSON let one bad UUID 500 every capture); contact PATCH gated `LEAD_VIEW_ALL` like contact GET (contact ids leak via `LeadOut.contactId`); round-robin per-candidate count query collapsed to one GROUP BY; `ix_contacts_tenant_email` is an expression index on `lower(email)` + `ContactUpdate` normalizes email so every write path agrees with it; capture form max-age bound added; round-robin race documented as accepted best-effort; dead `has_agent_response` removed. Deferred: SMS/WhatsApp drip steps (no adapter), territory assignment (§8.5), team-scoped `team_lead` visibility (§8.5 membership), inbound-reply webhooks, ML scoring (§8.18). 134 tests green; ruff + mypy strict clean.
- **Part 9 — Agents & teams (§8.5, done, 2026-07-17):** `modules/agents` — migration 0008 (three tenant-RLS tables: `agent_profiles` with i18n bio, controlled-vocabulary specialties, `service_areas geometry(MultiPolygon,4326)` + GiST, photo pipeline columns, unique per-tenant slug/user; `teams`; `team_members` with `tenant_id` added for RLS even though §6.2 omits it). Shared geometry helpers promoted from `listings/geo.py` to `app/common/geo.py` (+ MultiPolygon WKT build/parse — rings only ever built from schema-validated floats, same stance as Part 7's `inPolygon`). Profiles: agents create/edit their own; new `Permission.AGENT_MANAGE` (admin + team_lead) manages any; `is_published` is manager-gated (curated directory, mirrors listings' `featured`); slug/user conflicts surface as 409 via IntegrityError. Public: `GET /agents` directory (published only, keyset, specialty filter, display name joined via new batch `UserService.identities_for` — a profile whose account was disabled drops off rather than rendering a card with no person) and `GET /agents/{slug}` with the agent's published listings. Photo: slim §8.2 pipeline (presign → confirm → `process_agent_photo` on the `media` queue; shared helpers extracted from the media task into `workers/tasks/images.py`; avatar 320/card 640 webp+jpeg, EXIF stripped, no blurhash; replacement/delete clean up old objects post-commit via the existing `delete_media_objects`). Teams: admin creates/deletes/reassigns-lead; a team's `lead_user_id` manages its membership (foreign team_lead gets 404, no oracle); members must be active agent/team-lead accounts. **Deferred features from Part 8 landed:** (1) territory assignment — `TERRITORY` write-boundary rejection replaced by a has-territory-data check; `assign_lead` resolves the lead's point via new `ListingService.point_for`, gets candidates from `AgentsService.candidates_for_point` (ST_Contains over published service areas), re-validates active, picks least-loaded reusing `open_lead_counts`; no listing/point/match → unassigned (escalation sweep covers it). (2) team-scoped `team_lead` visibility — repository scoping generalized `scope_user_id` → `scope_user_ids` (IN) in listings+leads; service scope helpers went async: ADMIN/MARKETING tenant-wide, TEAM_LEAD = self ∪ `AgentsService.team_scope_user_ids` (members of teams they lead), AGENT = self; `MANAGES_ALL_ROLES` (action gates) deliberately still includes TEAM_LEAD — visibility (`TENANT_WIDE_ROLES`) and permission gates split. Key wiring decision: `AgentsService` takes optional storage/settings with a `build_agents_boundary(session)` factory so listings/leads services can inject the boundary accessors without needing `request.app.state` (their `get_*_service(session)` factories keep their signature). Stats v1: `GET /portal/agents/{profile_id}/stats` (self or AGENT_MANAGE) — listings by status, leads by stage, avg first-response seconds; commissions (§8.13), tours (§8.7), reviews (§8.11) join later. Existing tests updated: team_lead-tenant-wide assertions in test_listings now assert 404-without-membership / 200-with. Deferred: directory SEO in sitemap (content part), agent performance rollups (§8.15), review aggregation (§8.11). 144 tests green; ruff + mypy strict clean.
- **Part 10 — Favorites, saved searches & alerts (§8.9, done, 2026-07-17):** `modules/favorites` — migration 0009 (two tenant-RLS tables: `favorites` with unique `(tenant_id, user_id, listing_id)`; `saved_searches` owned by exactly one of `user_id` or `email` — CHECK `owner_xor_email` — storing the *validated camelCase dump* of `PublicListingFilters` in JSONB plus the creation-time `locale` so FTS `q` terms replay under the same text-search config, `frequency instant|daily|weekly`, `last_run_at` watermark, `is_active`). New authenticated surface `/api/v1/me/...` (first buyer-facing prefix — any tenant account operates on its own rows, ownership is the authorization, no RBAC permission): idempotent `PUT/DELETE /me/favorites/{listing_id}` (ON CONFLICT DO NOTHING; only published inventory, resolved via `ListingService.get_public`), `GET /me/favorites` (keyset over favorites; cards join through new boundary `ListingService.published_by_ids` — unpublished/deleted listings drop out, row survives for relist), saved-search CRUD capped at 20/user. Anonymous signup: public `POST /saved-searches` (rate-limited, honeypot + renderedAt like lead capture) persists inactive → Redis single-use confirm token (GETDEL) → `POST /saved-searches/confirm` activates *and* creates the `search_signup` lead via new `LeadsService.register_signup_lead` (capture trunk extracted to `_create_captured_lead`; the opt-in is the marketing consent). Unsubscribe: stateless HMAC token (`core/security.py sign_value/unsign_value`, purpose-domain-separated — must outlive any Redis TTL, §10.12) in every alert email → public `POST /saved-searches/unsubscribe`, idempotent. Alerts: `ListingService.transition` enqueues `match_published_listing` post-commit on `→ published` (lazy task import — the task module imports favorites' service which imports listings'); the task re-validates each stored filter (failure deactivates the row rather than crashing the sweep) and asks new boundary `ListingService.published_matches` (EXISTS on the Part 7 `_published_filtered` builder — matching can never disagree with search). Digests: daily Beat tick (07:00 UTC) runs daily rows every day + weekly rows on Mondays via `list_public(published_since=watermark)`; watermark starts at the search's `created_at` (no back-catalog dump) and always advances — reruns idempotent, same stance as `flag_stale_listings`. Both tasks route to `default` (human-facing email). Gotcha: Mailpit accumulates across pytest runs while `_clean_state` only resets DB+Redis — email-count assertions must be deltas, not absolutes. Mid-part incident: `core/config.py` reverted on disk to a Part-3 state (external cause, likely OneDrive sync) — restored from HEAD; watch for OneDrive reverting working-tree files. Deferred: SMS/WhatsApp alert channels, alert fan-out via a notifications module (§8.12), saved-search UI params beyond §8.3. 157 tests green; ruff + mypy strict clean.
- **Part 11 — WhatsApp handoff (§8.6, rescoped, done, 2026-07-17):** §8.6's in-app WebSocket chat replaced — after walking the UX with the user — by a **wa.me deep-link handoff**: the public widget POSTs the click, a lead lands in the CRM *before* the visitor leaves for WhatsApp, and the conversation itself happens outside our system (no conversations/messages tables, no WS/pub-sub, no contact-info regex detection; the original in-app design is preserved in `~/.claude/plans/continue-sunny-hollerith-agent-adf3000e7ac5ea94b.md` if ever revisited). Built as a small extension of `modules/leads` + `modules/agents`, not a new module. Migration 0010: nullable `agent_profiles.whatsapp_number` (E.164, `^\+[1-9]\d{6,14}$` in schemas; portal-only — never on `PublicAgentOut`, the server mints the URL). New public `POST /leads/capture/whatsapp-click` (same `_capture_limit` bucket, honeypot + `renderedAt` via shared `_CaptureBase` — `LeadCaptureCreate` now subclasses it, source fixed server-side): resolves the number as listing's assigned agent's profile number (new boundary `AgentsService.whatsapp_number_for`) → tenant `settings.contact.whatsapp_number` fallback → 409 when neither (losing the contact channel silently is worse than failing loudly); free-form settings values are reduced to digits, empty = unconfigured (no dead wa.me links); prefill carries `reference_code` + default-locale title; then the existing `_create_captured_lead` trunk with new `LeadSource.WHATSAPP_CLICK` (source weight 30 — warm intent) so scoring/assignment/drip/speed-to-lead all apply unchanged. Honeypot returns a *real* URL with a fake id — a bot sees nothing different, nothing persists. Plain request-info fallback needs no new endpoint (`POST /leads/capture` already covers it); agency-side visibility is the existing lead inbox filtered by source. Tests in new `tests/test_whatsapp_click.py` (leaf module — `test_agents` already imports from `test_leads`). 162 tests green; ruff + mypy strict clean.
